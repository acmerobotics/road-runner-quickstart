package org.firstinspires.ftc.teamcode.Kotlin_Bromine_Arya.Opmodes.Subsystems.CameraAutoMove;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

@TeleOp(name="DistanceFromTag", group="Linear OpMode")
public class DistanceFromTag extends LinearOpMode {

    //private final robot robot = robot.getHardware();
    //ArrayList<DistanceFromBoard.AttachmentNames> attachmentNames = new ArrayList<>();

    @Override
    public void runOpMode() {
/*
        robot.init(hardwareMap);

        Gamepad currentGamepad1 = new Gamepad();
        Gamepad previousGamepad1 = new Gamepad();


        waitForStart();
        while (opModeIsActive()) {


            if(gamepad2.y){
                Shoulder.shoulderPos = Shoulder.ShoulderPos.Raised;
                Wrist.wristPos = Wrist.wristPos.Raised;
            }
            if(gamepad2.a) {
                Shoulder.shoulderPos = Shoulder.ShoulderPos.Lowered;
                Wrist.wristPos = Wrist.wristPos.Raised;
            }
            if (gamepad2.x) {
                Wrist.wristPos = Wrist.wristPos.Lowered;
                Shoulder.shoulderPos = Shoulder.ShoulderPos.Lowered;
                ColorSensorClaw.CheckForRecognition();
            }

            //Choose what attachments are being considered
            attachmentNames.add(DistanceFromBoard.AttachmentNames.Wrist);
            //attachmentNames.add(DistanceFromBoard.AttachmentNames.Shoulder);

            //Check Shoulder Distance From Board
            telemetry.addData("Distance From Board of Shoulder", DistanceFromBoard.DistanceFTAG(attachmentNames));
            telemetry.update();
        }

    }


 */

    }
}
